{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react",
      "**/react-dom"
    ]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "build": "lerna run build"
  },
  "command": {
    "run": {
      "npmClient": "yarn"
    }
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
